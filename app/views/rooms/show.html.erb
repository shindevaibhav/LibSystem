<p id="notice"><%= notice %></p>

<p>
  <strong>Room number:</strong>
  <%= @room.room_number %>
</p>

<p>
  <strong>Size:</strong>
  <%= @room.size %>
</p>

<p>
  <strong>Building:</strong>
  <%= @room.building %>
</p>

<% if admin_logged_in? %>
<%= link_to 'Edit', edit_room_path(@room), class: "btn btn-lg btn-primary" %>
    <% end %>

<br>
<br>
  <%= link_to 'Back', rooms_path, class: "btn btn-lg btn-primary" %>

